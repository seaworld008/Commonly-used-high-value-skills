---
name: "weather"
description: "免 API Key 的天气查询技能，支持多数据源与自然语言请求。"
---

# Weather

用于天气查询、出行规划、活动排期等需要气象信息的场景。

## 安装

```bash
npx clawhub@latest install weather
```

## 特点

- 无需 API 密钥，开箱即用
- 结合 `wttr.in` 与 `Open-Meteo` 双数据源
- 支持自然语言查询与结构化输出
